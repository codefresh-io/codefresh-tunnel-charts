scaling:
  enabled: true
  # Redis parameters.
  
  redisURL: # String or map
    url: 'redis://test:6379'
    # Or secret key ref
    secretKeyRef:
      name:
      key:

plugin:
  env:
  - name: asd
    value: 'blah'

service:
  annotations:
    asd: 'dd'

tunnels:
  subdomainHost: ilia-tunnels.cf-op.com
  service:
    type: ClusterIP
    annotations:
      asd: 'dd'
  ingress:
    enabled: true
    ingressClassName: ""
    ingressAnnotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
    tls:
      enabled: false
      secretName: