apiVersion: v1
kind: Service
metadata:
  name: {{ include "codefresh-tunnel-server.fullname" . }}
  labels:
    {{- include "codefresh-tunnel-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codefresh-tunnel-server.selectorLabels" . | nindent 4 }}
