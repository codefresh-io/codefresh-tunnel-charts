apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "codefresh-tunnel-server.fullname" . }}-test-connection"
  labels:
    {{- include "codefresh-tunnel-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "codefresh-tunnel-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
